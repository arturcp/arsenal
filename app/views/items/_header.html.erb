<style>
 .main-banner {
    background-image: url("<%= campaign.main_image_url %>");
 }
</style>

<section class="main-banner parallax-bg bg-fixed overlay fullscreen-banner valign-wrapper mb-50" data-stellar-background-ratio="0.5" style="background-position: 0% 0%; height: 530px;">
  <div class="valign-cell">
    <div class="container padding-top-160">
      <div class="text-center">
          <h1 class="tt-headline loading-bar">
            <span><%= campaign.title %></span>
          </h1>

          <p class="sub-intro lead white-text"><%= campaign.subtitle %></p>
          <% if campaign.video_url %>
            <a class="btn popup-video mt-30 waves-effect waves-light" href="<%= campaign.video_url %>">
              Veja o vídeo
            </a>
          <% end %>
      </div>
    </div>
  </div>
</section>
