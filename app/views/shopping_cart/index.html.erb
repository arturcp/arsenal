<%= render 'checkout_header', campaign: @campaign %>

<h1 class="shopping-cart__title text-center">Carrinho de compras</h1>

<% if @shopping_cart.items.empty? %>
  <div class="card-panel purple lighten-5 empty-cart">Seu carrinho está vazio :(</div>
<% else %>
  <% @shopping_cart.items.each do |item| %>
    <%= render 'checkout_item', item: item %>
  <% end %>

  <%= render 'checkout_info', shopping_cart: @shopping_cart, campaign: @campaign %>
<% end %>

<%= render 'back_button' %>
