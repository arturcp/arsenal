<%= render 'checkout_header', campaign: @campaign %>

<h1 class="shopping-cart__title text-center">Itens no seu carrinho de compras</h1>

<% @shopping_cart.items.each do |item| %>
  <%= render 'checkout_item', item: item %>
<% end %>

<%= render 'checkout_info', shopping_cart: @shopping_cart %>
