<p id="notice"><%= notice %></p>

<h2><%= @article.title %></h2>

<div>
  <%= image_tag @article.main_image_url, class: 'img-fluid' if @article.main_image_url %>
</div>

<div>
  <%= raw(@article.description) %>
</div>

<%= link_to 'Edit', edit_article_path(@article) %> |
<%= link_to 'Back', articles_path %>
