$(document).ready(function() {
  // var modal = $('#closedCampaignModal');
  //
  // if (modal.length > 0) {
  //   modal.modal('open');
  // }

  // the "href" attribute of .modal-trigger must specify the modal ID that wants to be triggered
   $('.modal').modal();
});
